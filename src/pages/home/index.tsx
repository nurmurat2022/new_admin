import React from 'react'

import { Box } from '@mui/system'

const Home = () => {
  const email = localStorage.getItem('email')

  return (
    <Box sx={{ marginLeft: '167px' }}>
      test
    </Box>
  )
}

export default Home
